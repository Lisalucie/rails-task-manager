<br>
<h2>My tasks</h2>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task.id) %> -
    <% if task.completed%>
    <%= "finished !" %>
    <% else  %>
    <%= "to do" %>
    <%end %></li>
  <% end %>
</ul>
<br>
<%= link_to "Add a new Task", new_task_path %>
