<br>
<h2>Edit your task:</h2>

<%= form_for(@task) do |f| %>
 <p> <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :details %>
  <%= f.text_field :details %>
  <%= f.label :completed %>
  <%= f.check_box :completed %></p>
  <p><%= f.submit %></p>
<% end %>
<br>
<%= link_to "Cancel", tasks_path(@tasks) %>
